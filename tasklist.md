1. ~~add configurable space background: set a background image dir at public/img/backgrounds/ then let the user pick from that list: module reads the folder on startup, then populates the html with the found images; I can see that the background image should rotate with the `globe`~~ done — implemented as `presets/backgrounds.js` (a preset registry, like earthTextures.js, rather than a directory scan) with `config.background.{enabled,preset,imageUrl}`; renders as a huge textured sphere parented to the same rotating globe group so it spins in sync; the control panel's "Planet & Env" page exposes it as a single select (preset names, "Disabled" last).
2. ~~optimise control.html: split CSS out, separate pages per section, each property as a "discovered module"~~ done — control.html is now a redirect stub to `public/control/home.html`; the real pages (`home.html`/`planet-env.html`/`layers.html`) share `style.css` + `core.js`, and every config panel (theme, texture, background, camera, rotation-speed, atmosphere, day-night, clouds) is its own ES module under `public/control/panels/`, discovered via `data-panel="<name>"` attributes rather than hand-wired into one script. See "Live tuning" in README.md for the full layout.
