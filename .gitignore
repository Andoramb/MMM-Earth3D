node_modules/
*.log
.DS_Store
.claude

# User-created themes (control.html's Duplicate/Save/Delete buttons) - see
# node_helper.js's USER_THEMES_FILE comment. Never the shipped defaults in
# presets/themes.js, which stays tracked normally.
presets/themes-user.js
